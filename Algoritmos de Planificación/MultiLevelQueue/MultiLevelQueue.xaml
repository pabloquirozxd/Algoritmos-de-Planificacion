<UserControl x:Class="Algoritmos_de_Planificación.MultiLevelQueue.MultiLevelQueue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="Simulador Multi Level Queue - Hospital" FontSize="30" FontWeight="Bold" Foreground="{StaticResource ColorPrimario}"/>
                <TextBlock Text="Los pacientes se separan en colas fijas según la gravedad. Puedes ejecutar MLQ no expropiativo o expropiativo." FontSize="15" Foreground="{StaticResource TextoClaro}" Margin="0,8,0,0"/>
                <TextBlock Text="Cola 1 = Emergencia grave | Cola 2 = Urgencia media | Cola 3 = Consulta normal | Dentro de cada cola se aplica FCFS" FontSize="13" Foreground="{StaticResource TextoClaro}" Margin="0,5,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,28">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <TextBlock Text="Registrar paciente" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}" Margin="0,0,0,14"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="1.5*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="Paciente" FontWeight="SemiBold" Foreground="{StaticResource TextoOscuro}"/>
                        <TextBox x:Name="txtPaciente" Height="38" Margin="0,6,0,0" Padding="8" Background="{StaticResource InputColor}" BorderBrush="{StaticResource BordesSuaves}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="12,0,12,0">
                        <TextBlock Text="Caso médico" FontWeight="SemiBold" Foreground="{StaticResource TextoOscuro}"/>
                        <TextBox x:Name="txtCaso" Height="38" Margin="0,6,0,0" Padding="8" Background="{StaticResource InputColor}" BorderBrush="{StaticResource BordesSuaves}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="12,0,12,0">
                        <TextBlock Text="Arrival Time" FontWeight="SemiBold" Foreground="{StaticResource TextoOscuro}"/>
                        <TextBox x:Name="txtArrivalTime" Height="38" Margin="0,6,0,0" Padding="8" Background="{StaticResource InputColor}" BorderBrush="{StaticResource BordesSuaves}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" Margin="12,0,12,0">
                        <TextBlock Text="Burst Time" FontWeight="SemiBold" Foreground="{StaticResource TextoOscuro}"/>
                        <TextBox x:Name="txtBurstTime" Height="38" Margin="0,6,0,0" Padding="8" Background="{StaticResource InputColor}" BorderBrush="{StaticResource BordesSuaves}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4" Margin="12,0,0,0">
                        <TextBlock Text="Cola hospitalaria" FontWeight="SemiBold" Foreground="{StaticResource TextoOscuro}"/>
                        <ComboBox x:Name="cmbCola" Height="38" Margin="0,6,0,0" Padding="6" Background="{StaticResource InputColor}" BorderBrush="{StaticResource BordesSuaves}" SelectedIndex="0">
                            <ComboBoxItem Content="1 - Emergencia grave"/><ComboBoxItem Content="2 - Urgencia media"/><ComboBoxItem Content="3 - Consulta normal"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2" Margin="0,18,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <Button x:Name="btnAgregar" Grid.Column="0" Content="Agregar paciente" Height="40" Margin="0,0,8,0" Style="{StaticResource MenuButtonStyle}" Click="btnAgregar_Click"/>
                    <Button x:Name="btnEjecutarNoExpropiativo" Grid.Column="1" Content="MLQ No expropiativo" Height="40" Margin="8,0,8,0" Style="{StaticResource MenuButtonStyle}" Click="btnEjecutarNoExpropiativo_Click"/>
                    <Button x:Name="btnEjecutarExpropiativo" Grid.Column="2" Content="MLQ Expropiativo" Height="40" Margin="8,0,8,0" Style="{StaticResource MenuButtonStyle}" Click="btnEjecutarExpropiativo_Click"/>
                    <Button x:Name="btnEjemplo" Grid.Column="3" Content="Cargar ejemplo" Height="40" Margin="8,0,8,0" Style="{StaticResource MenuButtonStyle}" Click="btnEjemplo_Click"/>
                    <Button x:Name="btnLimpiar" Grid.Column="4" Content="Limpiar" Height="40" Margin="8,0,0,0" Style="{StaticResource MenuButtonStyle}" Click="btnLimpiar_Click"/>
                </Grid>
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,25">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="360"/></Grid.RowDefinitions>
                <TextBlock Text="Colas de pacientes" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}" Margin="0,0,0,12"/>
                <DataGrid x:Name="dgPacientes" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" BorderBrush="{StaticResource BordesSuaves}" GridLinesVisibility="Horizontal" RowHeight="38" FontSize="13" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Proceso" Binding="{Binding Proceso}" Width="80"/>
                        <DataGridTextColumn Header="Paciente" Binding="{Binding Paciente}" Width="120"/>
                        <DataGridTextColumn Header="Caso" Binding="{Binding Caso}" Width="180"/>
                        <DataGridTextColumn Header="Cola" Binding="{Binding Cola}" Width="160"/>
                        <DataGridTextColumn Header="Arrival" Binding="{Binding ArrivalTime}" Width="80"/>
                        <DataGridTextColumn Header="Burst" Binding="{Binding BurstTime}" Width="80"/>
                        <DataGridTextColumn Header="Completion" Binding="{Binding CompletionTime}" Width="100"/>
                        <DataGridTextColumn Header="Waiting" Binding="{Binding WaitingTime}" Width="90"/>
                        <DataGridTextColumn Header="Turnaround" Binding="{Binding TurnaroundTime}" Width="110"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="130"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Grid Grid.Row="3" Margin="0,0,0,25">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,18,0">
                    <TextBlock Text="Promedio Waiting Time" FontWeight="Bold" Foreground="{StaticResource ColorPrimario}"/>
                    <TextBlock x:Name="txtPromedioEspera" Text="0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}" Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="18,0,18,0">
                    <TextBlock Text="Promedio Turnaround Time" FontWeight="Bold" Foreground="{StaticResource ColorPrimario}"/>
                    <TextBlock x:Name="txtPromedioRetorno" Text="0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}" Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="18,0,18,0">
                    <TextBlock Text="Paciente actual" FontWeight="Bold" Foreground="{StaticResource ColorPrimario}"/>
                    <TextBlock x:Name="txtPacienteActual" Text="Ninguno" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}" Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="18,0,0,0">
                    <TextBlock Text="Modo actual" FontWeight="Bold" Foreground="{StaticResource ColorPrimario}"/>
                    <TextBlock x:Name="txtColaActual" Text="Ninguno" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="4" Margin="0,0,0,20">
                <TextBlock Text="Diagrama de Gantt" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextoOscuro}" Margin="0,0,0,10"/>
                <TextBlock x:Name="txtDescripcionGantt" Text="Muestra cómo el hospital atiende a los pacientes según el modo seleccionado." FontSize="13" Foreground="{StaticResource TextoClaro}" Margin="0,0,0,10"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="spGantt" Orientation="Horizontal" MinHeight="80"/>
                </ScrollViewer>
                <TextBlock x:Name="txtExplicacionFinal" Text="No expropiativo: no interrumpe. Expropiativo: sí interrumpe si llega una cola de mayor prioridad." Foreground="{StaticResource TextoClaro}" FontSize="13" Margin="0,18,0,0" TextAlignment="Center"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
